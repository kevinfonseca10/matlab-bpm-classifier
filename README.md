# matlab-bpm-classifier
A MATLAB Live Script that estimates the BPM (tempo) of .mp3 files using energy-based onset detection, categorizes them into custom playlists, and includes an interactive CLI player.
